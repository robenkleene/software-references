# OmniOutliner Settings

- `Preferences > Keyboard > New rows are created: Always at the same level`: The default behavior creates a new child if the current item has children, which is redundant because that's what `⇧⌘}` also does (whereas there's no shortcut to create a sibling of the current item)
